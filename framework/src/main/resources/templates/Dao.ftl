package ${package_name}.dao;

import com.lp.first.framework.crud.dao.BaseDao;
import org.springframework.stereotype.Repository;
import ${package_name}.model.${model_name};

/**
 * @author ${author}
 * @date ${date}
 */
@Repository
public interface ${model_name}Dao extends BaseDao<${model_name}, String>{

}