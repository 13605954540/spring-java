man:
  name: name o
  age: 1

spring:
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848         #Nacos????????
        file-extension: yaml
    loadbalancer:
      ribbon:
        enabled: false
